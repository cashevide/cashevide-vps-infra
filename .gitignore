# Real nginx configs (VPS-specific, contains actual domains)
conf.d/*.conf

# But keep the folder tracked in git
!conf.d/.gitkeep

# SSL certificates (secrets - never commit these)
certs/*
!certs/.gitkeep

# Environment files if any get added later
.env
.env.*

# OS/editor noise
.DS_Store
*.swp